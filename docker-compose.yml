version: '3.8'
services:
  postgres:
    image: postgres:latest
    env_file: .env
    volumes:
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    
